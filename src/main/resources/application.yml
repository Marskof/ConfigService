spring:
  application:
    name: ConfigService
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Marskof/ConfigService.git
          clone-on-start: true
          default-label: master
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
